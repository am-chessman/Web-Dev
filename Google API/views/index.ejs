<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NASA</title>
    <style>
        body {
            margin: 0;
            height: 100vh;
            width: 100vw;
            display: flex;
            justify-content: center;
            align-items: center;
        }     

        img {
            width: 100%;
        }
    </style>
</head>
<body>
    <% if (locals.apiData) { %>
        <div style="width: inherit; height: inherit; background-image: url(<%=apiData.url%>); background-position: center center; background-repeat: no-repeat;">
        </div>
    <% } %>
    </div>
</body>
</html>